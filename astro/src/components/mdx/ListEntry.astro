---
import { cva } from "@/src/cva.config"

const styles = cva({ base: "antialiased [&_p]:mb-0" })

const { class: className, ...props } = Astro.props
---

<li class={styles({ className })} {...props}><slot /></li>
