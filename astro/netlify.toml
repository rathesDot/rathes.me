# This site's own Netlify config. Without it, Netlify falls back to the
# repo-root netlify.toml, which carries the *primary* (Next.js) site's
# proxy-to-Astro redirects — those don't belong here and would make this
# site try to proxy to itself.
[build]
  command = "astro build"
  publish = "dist"
